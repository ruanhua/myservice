server:
    port: 8072
    servlet:
        context-path: /myservice
spring:
    datasource:
        driver-class-name: com.mysql.cj.jdbc.Driver
        url: jdbc:mysql://127.0.0.1:3306/myservice?serverTimezone=GMT%2B8&useUnicode=true&characterEncoding=UTF-8
#        url: jdbc:mysql://123.56.161.89:3306/myservice?serverTimezone=GMT%2B8&useUnicode=true&characterEncoding=UTF-8
        username: ruanhua
        password: ruanhua2008
        hikari:
            maximum-pool-size: 12 #最大连接数，小于等于0会被重置为默认值10；大于零小于1会被重置为minimum-idle的值
            connection-timeout: 60000  #连接超时时间:毫秒，小于250毫秒，否则被重置为默认值30秒
            minimum-idle: 10  #最小空闲连接，默认值10，小于0或大于maximum-pool-size，都会重置为maximum-pool-size
            idle-timeout: 500000  #空闲连接超时时间，默认值600000（10分钟），大于等于max-lifetime且max-lifetime>0，会被重置为0；不等于0且小于10秒，会被重置为10秒。
            max-lifetime: 540000  #连接最大存活时间.不等于0且小于30秒，会被重置为默认值30分钟.设置应该比mysql设置的超时时间短
            connection-test-query: SELECT 1    #连接测试查询

    redis:
        host: 123.56.161.89
        port: 6379
        password: Zt*Q8^4j
        timeout: 10000
        lettuce:
            pool:
                max-active: 8
                max-wait: -1ms
                min-idle: 0
                max-idle: 8

mybatis-plus:
    type-aliases-package: org.snaker.engine.entity
    configuration:
        log-impl: org.apache.ibatis.logging.stdout.StdOutImpl #开启sql日志
        # 该配置就是将带有下划线的表字段映射为驼峰格式的实体类属性
        map-underscore-to-camel-case: true

springdoc:
    packagesToScan: com.cn.myservice.controller

logging:
    level:
        cn.taikang.message.core: debug
